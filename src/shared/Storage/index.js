//  LIST OF ALL STORAGE KEYS
// USE @ after every key name
export const STR_KEYS = {
  USERTOKEN: '@UserToken',
  USERDATA: '@UserData',
  COUNTRY_CODE: '@CountryCode',
  PROCEED_STATUS: '@ProceedStatus',
  FCM_TOKEN: '@fcmToken',
  VERIFIED_NUMBER: '@verifiedNumber',
};

// export const StoreLocalData = async (KEY, VALUE, resp,) => {
//     try {
//         await AsyncStorage.setItem(KEY, VALUE)
//     } catch (e) {
//         // saving error
//     }
// }
